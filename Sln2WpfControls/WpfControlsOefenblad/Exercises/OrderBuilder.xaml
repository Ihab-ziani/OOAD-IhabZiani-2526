<Page x:Class="WpfControlsOefenblad.Exercises.OrderBuilder"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WpfControlsOefenblad.Exercises"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderBuilder">

    <Grid Background="#eeeeee">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="220"/>
        </Grid.RowDefinitions>

        <Frame x:Name="fraAssignment" Grid.Row="0" />

        <Border Background="White" Grid.Row="1">
            <!-- HIERONDER BEGINT XAML VAN DE OEFENING -->
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Kolom 1: Ingrediënten -->
                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Ingrediënten" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <CheckBox x:Name="chkCheese" Content="Cheese" IsChecked="True" Margin="0,0,0,5"/>
                    <CheckBox x:Name="chkTomato" Content="Tomato" Margin="0,0,0,5"/>
                    <CheckBox x:Name="chkPepperoni" Content="Pepperoni" IsChecked="True" Margin="0,0,0,5"/>
                </StackPanel>

                <!-- Kolom 2: Extra toppings + buttons -->
                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="Extra Toppings" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <CheckBox x:Name="chkMushrooms" Content="Mushrooms" Margin="0,0,0,5"/>
                    <CheckBox x:Name="chkOnions" Content="Onions" Margin="0,0,0,5"/>
                    <CheckBox x:Name="chkOlives" Content="Olives" Margin="0,0,0,5"/>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
                        <Button x:Name="btnConfirm" Content="Bevestig" Padding="10,5" Margin="0,0,5,0" Click="btnConfirm_Click"/>
                        <Button x:Name="btnReset" Content="Reset" Padding="10,5" Click="btnReset_Click"/>
                    </StackPanel>
                </StackPanel>

                <!-- Kolom 3: Levering + samenvatting -->
                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="Levering" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <RadioButton x:Name="rbDelivery" Content="Thuislevering" GroupName="Delivery" Margin="0,0,0,5"/>
                    <RadioButton x:Name="rbPickup" Content="Afhalen" GroupName="Delivery" Margin="0,0,0,5"/>
                    <TextBlock x:Name="txtSummary" Text="..." Margin="0,20,0,0" TextWrapping="Wrap"/>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</Page>
